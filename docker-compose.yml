services:
  bca_funding_bot:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SESSION_SECRET=CHANGE_ME_SESSION_SECRET
      - DATABASE_URL=sqlite:///./funding.db
    volumes:
      - ./funding.db:/app/funding.db
